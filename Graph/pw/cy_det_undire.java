package Graph.pw;

public class cy_det_undire {
    
    public static void main(String[] args) {
        /*
         public static boolean dfs(int s,int p, boolean vis[], ArrayList<ArrayList<Integer>> adj){
        vis[s]=true;
        for(int i:adj.get(s)){ //call dfs on connection of s
            if(vis[i]==false){ //if connection unvisited,go dfs
               if(dfs(i,s,vis,adj))
                return true;
            }
            else if(vis[i]==true && i!=p){  //if vertex is visited but not parent then cycle detectd
                return true;
            }
        }
        return false;  //cycle not detected
    }
   / public boolean isCycle(int V, ArrayList<ArrayList<Integer>> adj) {
        // Code here
        boolean vis[]=new boolean[V];
        for(int i=0;i<V;i++){
            if(vis[i]==false){  //call only for unvisited vertex
                if(dfs(i,-1,vis,adj))
                  return true;
            }
        }
        return false;
    }
}
         */
    }
}

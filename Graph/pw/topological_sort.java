package Graph.pw;

public class topological_sort {
    public static void main(String[] args) {
        /*
         class Solution
{
    public static void dfs(int x,boolean vis[],Stack<Integer> st, ArrayList<ArrayList<Integer>>adj){
        vis[x]=true;
        for(int i:adj.get(x)){ //call connection of x
            if(vis[i]==false)
             dfs(i,vis,st,adj);
        }
        st.push(x);
    }
    //Function to return list containing vertices in Topological order. 
    static int[] topoSort(int V, ArrayList<ArrayList<Integer>> adj) 
    {
        boolean vis[]=new boolean[V];
        int ans[]=new int[V];
        Stack<Integer> st=new Stack<>();
        for(int i=0;i<V;i++){
            if(vis[i]==false)
             dfs(i,vis,st,adj);
         
        }
        int i=0;
        while(!st.isEmpty()){
            ans[i]=st.pop();
            i++;
        }
        return ans;
    }
}w
         */
    }
}
